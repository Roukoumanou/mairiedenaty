{% extends 'admin/base.html.twig' %}

{% block title %}{{ member.fullName|title }}{% endblock %}

{% block body %}
    <h1>{{ member.fullName|title }}</h1>

    <div class="row m-5 mb-5">
        <div class="col-lg-4">
            <img class="card-img-top img-fluid" src="{{ vich_uploader_asset(member, 'imageFile') }}" alt="{{ member.fullName|title }}">
        </div>
        <div class="col-lg-5">
            {{ member.bio|nl2br }}
            <table class="table table-hover bg-light">
                <tbody>
                    <tr>
                        <th><span>Nom Prénom</span></th>
                        <td><span>{{ member.fullName|title }} Fcfa</span></td>
                    </tr>
                    <tr>
                        <th><span>Titre</span></th>
                        <td><span>{{ member.title|title }} Fcfa</span></td>
                    </tr>
                    <tr>
                        <th><span>Mandature</span></th>
                        <td><span>{{ member.mandature }}</span></td>
                    </tr>
                </tbody>
            </table>
    </div>
</div>
{% endblock %}
