<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
  <a class="navbar-brand" href="{{ path('home') }}">Acceuil</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor02">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link" href="{{ path('demarches') }}">Mes Démarches</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('projects') }}">Les Projets</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('conseils_communaux') }}">Conseils Communaux</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('infos') }}">News</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('mot_du_maire') }}">Mot Du Maire</a>
        </li>
      </ul>
      <form class="d-flex">
          <input class="form-control me-sm-2" id="search" type="text" placeholder="Faire une recherche"/>
      </form>
        {% if app.user %}
        {% set user = app.user %}
            <!-- Topbar Navbar -->
            <ul class="navbar-nav float rigth">

                <div class="topbar-divider d-none d-sm-block"></div>

                <!-- Nav Item - User Information -->
                <li class="nav-item dropdown no-arrow">
                <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <span class="mr-2 d-none d-lg-inline text-gray-600 small">{{ user.fullName }}</span>
                    <img class="img-profile rounded-circle" src="https://source.unsplash.com/random/60x60">
                </a>
                <!-- Dropdown - User Information -->
                <div class="dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="userDropdown">
                    <a class="dropdown-item" href="{{ path('profil') }}">
                    Votre Tableau de Bord
                    </a>
                    <a class="dropdown-item" href="{{ path('account_update') }}">
                    Modifier Votre Compte
                    </a>
                    <a class="dropdown-item" href="{{ path('password_edit') }}">
                    Modifier Votre Mot De Passe
                    </a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('app_logout') }}">
                    Deconnexion
                    </a>
                </div>
                </li>

            </ul>
        {% else %}
        <ul class="navbar-nav float rigth">
            <li class="nav-item"><a class="nav-link active" href="{{ path('app_login') }}">Connexion</a></li>
            <li class="nav-item"><a class="nav-link active" href="{{ path('app_register') }}">Inscription</a></li>
        </ul>
        {% endif %}
    </div>
  </div>
</nav>

